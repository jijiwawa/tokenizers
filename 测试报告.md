# tokenizers 单线程 benchmark 测试报告

## 测试范围

- 仓库：`/Users/a111/Desktop/code/tokenizers`
- 提交：当前工作区 `99bc8f5b` 基础上叠加本次优化改动
- Python 环境：`tokenizers-bench`
- 构建方式：`maturin develop --manifest-path bindings/python/Cargo.toml --release`
- benchmark 脚本：[repeat_single_thread_benchmark.py](/Users/a111/Desktop/code/tokenizers/bindings/python/benches/repeat_single_thread_benchmark.py)

## benchmark 配置

- 线程数：`1`
- warm-up：`1`
- 正式轮次：`10`
- 数据集：`facebook/xnli`
- split：`train`
- 文档数：`10000`
- 文档模式：`long`
- 数据量：`24.04 MB`
- 本地模型：
  - [tokenizer.json](/Users/a111/Desktop/code/tokenizers/llama3_tokenizer/LLM-Research/Meta-Llama-3___1-8B/tokenizer.json)
  - [tokenizer.model](/Users/a111/Desktop/code/tokenizers/llama3_tokenizer/LLM-Research/Meta-Llama-3___1-8B/original/tokenizer.model)

## 功能测试

执行命令：

```bash
/Users/a111/miniconda3/envs/tokenizers-bench/bin/python -m pytest \
  bindings/python/tests/bindings/test_tokenizer.py::TestTokenizer::test_encode \
  bindings/python/tests/bindings/test_tokenizer.py::TestTokenizer::test_encode_batch_fast_text_inputs \
  bindings/python/tests/bindings/test_tokenizer.py::TestTokenizer::test_multithreaded_concurrency
```

结果：

- `3 passed in 0.19s`

说明：

- `test_encode` 验证普通编码与批量文本输入基础行为
- `test_encode_batch_fast_text_inputs` 是本次新增回归测试，直接覆盖文本输入快路径
- `test_multithreaded_concurrency` 验证并发场景下 `encode_batch_fast` 没有被这次优化破坏

## benchmark 命令

优化前：

```bash
/Users/a111/miniconda3/envs/tokenizers-bench/bin/python \
  bindings/python/benches/repeat_single_thread_benchmark.py \
  --threads 1 --warmups 1 --runs 10 --engine both --json-output before_results.json
```

优化后：

```bash
/Users/a111/miniconda3/envs/tokenizers-bench/bin/python \
  bindings/python/benches/repeat_single_thread_benchmark.py \
  --threads 1 --warmups 1 --runs 10 --engine both --json-output after_results.json
```

原始结果文件：

- [before_results.json](/Users/a111/Desktop/code/tokenizers/before_results.json)
- [after_results.json](/Users/a111/Desktop/code/tokenizers/after_results.json)

## 性能结果

### Hugging Face tokenizers

优化前样本 `MB/s`：

`4.36 4.36 4.37 4.43 4.27 4.31 4.34 4.01 4.15 4.51`

优化后样本 `MB/s`：

`4.72 4.93 4.93 5.01 5.02 4.51 5.00 4.76 4.11 4.05`

统计对比：

| 指标 | 优化前 | 优化后 | 变化 |
| --- | ---: | ---: | ---: |
| mean MB/s | 4.31 | 4.70 | +9.17% |
| stdev | 0.14 | 0.37 | +0.23 |
| CV | 3.28% | 7.79% | +4.51 pct |
| min MB/s | 4.01 | 4.05 | +0.04 |
| max MB/s | 4.51 | 5.02 | +0.51 |

结论：

- 平均吞吐从 `4.31 MB/s` 提升到 `4.70 MB/s`
- 在当前测量口径下，单线程 `encode_batch_fast` 提升约 `9.17%`
- 优化后平均性能更高，但波动比 before 大
- 由于 after 里有两次较低样本，当前稳定性更接近“轻微波动”，还不是最理想状态

### tiktoken 参考值

优化前均值：

- `11.52 MB/s`

优化后均值：

- `12.21 MB/s`

说明：

- 本次源码修改不针对 `tiktoken`
- 这个差异更像同机测试噪声，不作为本次优化收益判断依据

## 改动归因

这次收益主要来自三部分：

1. Python 文本输入构造更轻
2. `PyEncoding` 频繁创建/销毁的成本下降
3. Rust `OffsetType::None` 路径减少了通用 iterator/collect 开销

按实现范围判断，第一部分大概率是主收益来源；后两部分属于叠加优化。

## 风险与说明

- 这次 benchmark 测的是 Python 端完整调用链，不是纯 Rust 核心吞吐
- 因此结果会同时受到 Python 对象创建、解释器调度和系统瞬时负载影响
- 如果后续要继续追到历史 `6.5+ MB/s`，建议下一步用更细粒度 profiler 分离：
  - Python 输入转换
  - `PyEncoding` materialization
  - `ByteLevel` / `NormalizedString` 内部开销

## 最终结论

本次优化已经完成源码落地、功能验证和同口径复测。

- 功能测试：通过
- 单线程 benchmark：提升
- 当前观测收益：`+9.17%`
- 当前状态：值得保留，并适合继续在这条路径上做第二轮优化
