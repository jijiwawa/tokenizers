# 跨服务器收益验证指南

这份指南用于在其他服务器上快速验证本次 `tokenizers` 优化收益，重点覆盖：

- 环境准备
- 基线版本与优化版本的对比方式
- 如何用同一套 benchmark 口径复测
- 如何减少服务器噪声，避免误判

## 适用版本

建议直接对比下面两个提交：

- 基线提交：`be1513cf`
  说明：包含第一轮 Python/Rust 优化，但不包含最新的 `ByteLevel` fast path
- 优化提交：`a73478b1`
  说明：在基线之上加入 `NormalizedString` 的 byte-level fast path

如果要验证“当前最新版本相对更早历史版本”的收益，也可以把基线替换成你关心的任意 commit。

## 目标 benchmark

统一使用仓库内脚本：

- [repeat_single_thread_benchmark.py](/Users/a111/Desktop/code/tokenizers/bindings/python/benches/repeat_single_thread_benchmark.py)

统一验证口径：

- 线程数：`1`
- warm-up：`1`
- runs：`10`
- engine：`huggingface`
- 数据：`facebook/xnli`
- 文档数：`10000`
- 文档模式：`long`

## 一次性准备

### 1. 安装基础依赖

至少需要：

- Rust toolchain
- Python 3.11
- `conda` 或其他隔离环境
- `maturin`

如果服务器在国内，建议同时配置：

- Cargo 镜像
- pip 国内镜像

### 2. 创建 Python 环境

```bash
conda create -n tokenizers-bench python=3.11 -y
conda activate tokenizers-bench
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple \
  maturin datasets tiktoken huggingface_hub pytest pytest-asyncio requests numpy
```

### 3. 准备模型文件

benchmark 默认依赖两个本地文件：

- `llama3_tokenizer/LLM-Research/Meta-Llama-3___1-8B/tokenizer.json`
- `llama3_tokenizer/LLM-Research/Meta-Llama-3___1-8B/original/tokenizer.model`

如果服务器上路径不同，可以在 benchmark 命令里通过参数覆盖：

- `--tokenizer-json`
- `--tiktoken-bpe`

## 推荐测试流程

### 方案 A：最快验证

适合只想快速判断“优化版是不是更快”。

#### 1. clone 仓库

```bash
git clone <your-repo-url> tokenizers
cd tokenizers
```

#### 2. 测基线版本

```bash
git checkout be1513cf
conda run -n tokenizers-bench maturin develop --manifest-path bindings/python/Cargo.toml --release

conda run -n tokenizers-bench python \
  bindings/python/benches/repeat_single_thread_benchmark.py \
  --threads 1 \
  --warmups 1 \
  --runs 10 \
  --engine huggingface \
  --json-output baseline.json
```

#### 3. 测优化版本

```bash
git checkout a73478b1
conda run -n tokenizers-bench maturin develop --manifest-path bindings/python/Cargo.toml --release

conda run -n tokenizers-bench python \
  bindings/python/benches/repeat_single_thread_benchmark.py \
  --threads 1 \
  --warmups 1 \
  --runs 10 \
  --engine huggingface \
  --json-output optimized.json
```

#### 4. 计算收益

```bash
python - <<'PY'
import json

with open("baseline.json", "r", encoding="utf-8") as f:
    baseline = json.load(f)["results"][0]["summary"]["mean_mb_s"]
with open("optimized.json", "r", encoding="utf-8") as f:
    optimized = json.load(f)["results"][0]["summary"]["mean_mb_s"]

uplift = (optimized / baseline - 1.0) * 100.0
print(f"baseline_mean={baseline:.2f} MB/s")
print(f"optimized_mean={optimized:.2f} MB/s")
print(f"uplift={uplift:.2f}%")
PY
```

## 方案 B：更稳的对比方式

适合服务器噪声较大，或者你要把结果拿给别人复核。

### 1. 用 worktree 隔离两个版本

```bash
git worktree add --detach ../tokenizers-base be1513cf
git worktree add --detach ../tokenizers-opt a73478b1
```

这样可以避免反复 `checkout`，也不容易污染当前目录。

### 2. 分别编译

```bash
cd ../tokenizers-base
conda run -n tokenizers-bench maturin develop --manifest-path bindings/python/Cargo.toml --release

cd ../tokenizers-opt
conda run -n tokenizers-bench maturin develop --manifest-path bindings/python/Cargo.toml --release
```

### 3. 分别跑 benchmark

基线：

```bash
cd ../tokenizers-base
conda run -n tokenizers-bench python \
  bindings/python/benches/repeat_single_thread_benchmark.py \
  --threads 1 \
  --warmups 1 \
  --runs 10 \
  --engine huggingface \
  --tokenizer-json /absolute/path/to/tokenizer.json \
  --tiktoken-bpe /absolute/path/to/tokenizer.model \
  --json-output baseline.json
```

优化版：

```bash
cd ../tokenizers-opt
conda run -n tokenizers-bench python \
  bindings/python/benches/repeat_single_thread_benchmark.py \
  --threads 1 \
  --warmups 1 \
  --runs 10 \
  --engine huggingface \
  --tokenizer-json /absolute/path/to/tokenizer.json \
  --tiktoken-bpe /absolute/path/to/tokenizer.model \
  --json-output optimized.json
```

## 如何减少测试噪声

为了让别的服务器上结果更可信，建议同时做下面几件事：

### 1. 固定单线程

脚本里已经会设置：

- `RAYON_NUM_THREADS=1`

但这只限制 Rust 内部线程数，不等于隔离 CPU 核。

### 2. 在 Linux 上固定 CPU

如果服务器是 Linux，建议额外加：

```bash
taskset -c 0 conda run -n tokenizers-bench python ...
```

如果是 NUMA 机器，还可以进一步加：

```bash
numactl --cpunodebind=0 --membind=0 taskset -c 0 conda run -n tokenizers-bench python ...
```

### 3. 尽量避免后台负载

测试时尽量不要同时跑：

- 编译
- 大文件拷贝
- 其他高 CPU 推理任务
- 自动备份/同步任务

### 4. 首次运行和正式运行分开

脚本默认有 `1` 次 warm-up，但如果机器缓存很冷，建议先手动跑一轮再开始正式记录。

### 5. 不要混用不同编译产物

每切换一个 commit，都要重新执行：

```bash
conda run -n tokenizers-bench maturin develop --manifest-path bindings/python/Cargo.toml --release
```

否则 Python 可能仍然加载的是上一版 `.so`。

## 如何判断结果是否有效

建议同时看两个指标：

### 1. 平均吞吐

- 直接看 `mean_mb_s`

### 2. 波动系数

- 看 `cv_percent`

一个简单口径：

- `CV <= 5%`：比较稳定
- `5% < CV <= 10%`：轻微波动，可接受
- `CV > 10%`：环境噪声偏大，建议重跑

## 预期结果

在我们当前机器上，这轮 Rust fast path 的结果是：

- 基线：约 `4.55 MB/s`
- 优化版：约 `7.00 MB/s`
- 提升：约 `+53.7%`

其他服务器上不一定复现完全一样的绝对值，但如果环境稳定，通常应该仍然能看到明显正收益。

## 常见问题

### 1. benchmark 很慢或者卡住

优先检查：

- `datasets` 是否首次下载缓存
- 模型文件是否走了远程下载
- Python 扩展是否重新编译完成

### 2. 两次结果差很多

优先检查：

- 是否切换 commit 后忘记重编译
- 是否和其他任务抢 CPU
- 是否没有固定 CPU / NUMA
- 是否混入了不同服务器的模型或数据路径

### 3. 明明切了 commit，结果没变化

最常见原因是 Python 还在加载旧的 `tokenizers.abi3.so`。

可以重新执行：

```bash
conda run -n tokenizers-bench maturin develop --manifest-path bindings/python/Cargo.toml --release
```

必要时重新开一个 shell 再测。

## 建议保留的结果文件

每次验证建议至少保留：

- `baseline.json`
- `optimized.json`
- 机器信息
- CPU 型号
- 是否固定 core
- 是否 NUMA 绑定
- Python 版本
- Rust 版本

这样后续跨机器比较时，不容易只看单个数字误判。
