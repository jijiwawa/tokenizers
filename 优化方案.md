# tokenizers 单线程 benchmark 优化方案

## 目标

针对本仓库当前最关注的用例做优化：

- 模型：本地 `Meta-Llama-3.1-8B` tokenizer
- 数据：`facebook/xnli` `train` split
- 输入规模：`10000` 条文档，约 `24.04 MB`
- 路径：Python `Tokenizer.from_file(...).encode_batch_fast(...)`
- 线程数：`1`

这条链路对应的是 [repeat_single_thread_benchmark.py](/Users/a111/Desktop/code/tokenizers/bindings/python/benches/repeat_single_thread_benchmark.py) 默认配置。

## 热点结论

基于此前对该用例做的本地 `sample` 采样，可以把热点分成两层：

1. Python binding 开销明显

- `encode_batch_fast` 需要先把 Python 输入转成 Rust `EncodeInput`
- 返回时又要把 Rust `Encoding` 包成大量 `PyEncoding`
- 当前 benchmark 每轮会创建并销毁 `10000` 个 `Encoding` Python 对象，这部分成本不小

2. Rust `OffsetType::None` 路径仍有额外开销

- `encode_batch_fast` 不需要字符 offsets，但仍会走 `PreTokenizedString::into_encoding(...)`
- 原实现通过 iterator + `collect()` 组装 `Encoding`
- 该分支还没有复用一个更轻的直接构造路径

## 已实施优化

### 1. Python 文本输入快路径

修改文件：

- [bindings/python/src/tokenizer.rs](/Users/a111/Desktop/code/tokenizers/bindings/python/src/tokenizer.rs)

优化点：

- 用 `PyString -> to_cow() -> owned String` 直接提取文本，减少通用 `extract::<String>()` 的额外开销
- 为 `list[str]` / `tuple[str, str]` 增加更直接的 owned input 构造逻辑
- 同步 `encode_batch` / `encode_batch_fast` 复用已有 owned input builder，避免重复走更重的提取分支

预期收益：

- 对当前 `list[str]` 单线程批量文本输入，预计带来 `3% ~ 8%` 提升

### 2. `PyEncoding` freelist

修改文件：

- [bindings/python/src/encoding.rs](/Users/a111/Desktop/code/tokenizers/bindings/python/src/encoding.rs)

优化点：

- 给 `PyEncoding` 增加 `freelist = 1024`
- 减少连续 benchmark 轮次里 `Encoding` Python 对象反复分配/释放的成本

预期收益：

- 对多轮重复 benchmark 更有效，预计 `1% ~ 4%`

### 3. `OffsetType::None` 直接构造 `Encoding`

修改文件：

- [tokenizers/src/tokenizer/encoding.rs](/Users/a111/Desktop/code/tokenizers/tokenizers/src/tokenizer/encoding.rs)
- [tokenizers/src/tokenizer/pre_tokenizer.rs](/Users/a111/Desktop/code/tokenizers/tokenizers/src/tokenizer/pre_tokenizer.rs)

优化点：

- 为 `Encoding` 增加内部 `push` 辅助方法，复用已有字段填充逻辑
- `OffsetType::None` 分支先统计 token 总数，再 `with_capacity` 一次性分配
- 用直接循环替代 `flat_map(...).collect()` 的通用路径
- 顺手把这个 fast path 上的 `word_idx` / `type_id` 传播补齐，避免该分支继续丢语义

预期收益：

- 对当前 benchmark 预计 `2% ~ 6%`

### 4. 可重复执行 benchmark 与回归测试

新增文件：

- [repeat_single_thread_benchmark.py](/Users/a111/Desktop/code/tokenizers/bindings/python/benches/repeat_single_thread_benchmark.py)
- [test_tokenizer.py](/Users/a111/Desktop/code/tokenizers/bindings/python/tests/bindings/test_tokenizer.py)

优化点：

- 新 benchmark 脚本固定了本地模型、单线程、warm-up、重复次数、JSON 输出
- 新增 `encode_batch_fast` 文本输入回归测试，覆盖本次优化的关键路径

## 收益预期汇总

对当前单线程用例，组合收益的保守预期是：

- `+5% ~ +12%`

这个区间来自热点占比和实现改动范围推断，不是理论上限。

## 后续可继续做的优化

下面这些还没做，但对这个用例仍然值得：

### 1. 为 benchmark / 服务侧增加“轻结果”接口

思路：

- 当前 `encode_batch_fast` 依然要返回完整 `Encoding`
- 如果调用方只关心 `ids` 或只做吞吐测试，`PyEncoding` 构建仍然偏重

收益预期：

- `+15% ~ +30%`

风险：

- 这会新增 Python API，属于功能设计变更，不适合在这一步直接塞进现有接口

### 2. 继续压缩 fast path 返回载荷

思路：

- 当前 fast path offsets 已经归零，但 `Encoding` 结构本身仍完整
- 可以继续评估是否给 fast path 做更轻的内部表示，再按需 materialize

收益预期：

- `+5% ~ +15%`

风险：

- 需要细查 `post_processor`、padding、overflowing 等兼容性

### 3. 跟进 `ByteLevel` / `NormalizedString` 分配

思路：

- 采样里还能看到 `ByteLevel::pre_tokenize` 和 `NormalizedString::transform_range`
- 如果继续追单线程极限，这一层值得用更细的 profiler 再拆

收益预期：

- `+5% ~ +10%`

风险：

- 改动面会明显大于这次，需要更完整的回归测试
