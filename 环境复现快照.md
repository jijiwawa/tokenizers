# tokenizers 环境复现快照

生成时间：

- `2026-04-08`

用途：

- 为后续在其他机器上复现 `tokenizers` 单线程 benchmark 提供环境参考
- 重点记录对性能敏感的硬件、系统、电源、swap、编译链和 Python/Rust 版本

说明：

- 本文档刻意省略了序列号、UUID 等和性能复现无关的敏感标识

## 1. 仓库状态

- 仓库路径：`/Users/a111/Desktop/code/tokenizers`
- 当前分支：`performance-optimization-v2`
- 当前提交：`a73478b16309f97f2556528b9f31ac8a2c6c80c4`
- 最近两个提交：
  - `a73478b1 Add byte-level normalized string fast path`
  - `be1513cf Optimize single-thread Python tokenizer benchmark`

## 2. 硬件信息

- 机型：`MacBook Pro`
- Model Identifier：`Mac17,2`
- 芯片：`Apple M5`
- 架构：`arm64`
- 总核心数：`10`
- Performance cores：`4`
- Efficiency cores：`6`
- 内存：`16 GB`
- `hw.memsize`：`17179869184` bytes

关键原始输出：

```text
Darwin Kernel: 25.0.0
Apple M5
hw.ncpu: 10
hw.physicalcpu: 10
hw.logicalcpu: 10
hw.perflevel0.physicalcpu: 4
hw.perflevel1.physicalcpu: 6
```

## 3. 操作系统与系统状态

- 系统：`macOS 26.0`
- Build：`25A8353`
- Kernel：`Darwin 25.0.0`
- Firmware：`13822.1.2`
- Boot Mode：`Normal`
- SIP：`Enabled`
- Secure Virtual Memory：`Enabled`

运行时状态：

- 开机时长：`23 days, 31 mins`
- 当前 load average：`2.46 2.76 3.04`
- 当前 swap：
  - total：`11264.00M`
  - used：`10218.50M`
  - free：`1045.50M`

说明：

- `swap` 使用量偏高，这会影响 benchmark 稳定性
- 如果在其他机器复现，建议记录并尽量降低 swap 压力

## 4. 电源与功耗相关配置

当前电源状态：

- 电源来源：`AC Power`
- 电池：`100%`
- low power mode：`0`

当前 `pmset` 中和性能较相关的配置：

- `lowpowermode 0`
- `powernap 0`
- `sleep 1`，但被 `runningboardd`, `powerd`, `mds_stores` 阻止
- `standby 0`

说明：

- 本次 benchmark 不是在电池供电或低电量模式下完成的
- 如果在别的 macOS 机器复现，建议尽量接电，并关闭低功耗模式

## 5. Shell 与环境变量

- 当前 shell：`zsh`
- `PATH` 关键顺序中包含：
  - `/opt/homebrew/bin`
  - `/Users/a111/miniconda3/bin`
  - `/Users/a111/.cargo/bin`

当前环境变量中与复现相关的状态：

- 当前激活 conda 环境：`base`
- 没有预设：
  - `RAYON_NUM_THREADS`
  - `PYTHONPATH`
  - `HTTP_PROXY`
  - `HTTPS_PROXY`
  - `ALL_PROXY`

说明：

- benchmark 脚本运行时会自行设置 `RAYON_NUM_THREADS`
- 本次测试没有依赖代理环境变量

## 6. Python / conda 环境

全局 Python：

- `python3 --version`：`Python 3.14.3`
- 路径：`/opt/homebrew/bin/python3`

conda：

- 版本：`25.11.1`
- 已存在环境：
  - `base`
  - `ann_test`
  - `ds-Engram`
  - `learning-agent`
  - `mlx-qwen`
  - `tokenizers-bench`

实际 benchmark 环境：

- 环境名：`tokenizers-bench`
- Python：`3.11.15`
- pip：`26.0.1`
- 环境路径：`/Users/a111/miniconda3/envs/tokenizers-bench`

## 7. benchmark 环境关键包版本

`tokenizers-bench` 中的关键包：

- `maturin 1.12.6`
- `datasets 4.8.4`
- `tiktoken 0.12.0`
- `huggingface-hub 1.9.2`
- `numpy 2.4.4`
- `pytest 9.0.3`
- `pytest-asyncio 1.3.0`
- `requests 2.33.1`
- `tokenizers 0.22.3.dev0`

当前 `tokenizers` Python 包加载位置：

- `/Users/a111/Desktop/code/tokenizers/bindings/python/py_src/tokenizers/__init__.py`

说明：

- 当前 benchmark 使用的是工作区本地 editable 安装版本，不是 PyPI 上的已发布 wheel

## 8. Rust / 构建链

Rust：

- `rustc 1.93.1 (01f6ddf75 2026-02-11)`
- `cargo 1.93.1 (083ac5135 2025-12-15)`
- `rustup 1.28.2 (e4f3ad6f8 2025-04-28)`
- active toolchain：`stable-aarch64-apple-darwin`

Clang / Apple toolchain：

- `Apple clang version 17.0.0 (clang-1700.6.3.2)`
- target：`arm64-apple-darwin25.0.0`
- `xcode-select -p`：`/Library/Developer/CommandLineTools`

Python 构建工具：

- `maturin 1.12.6`

## 9. Cargo 镜像与网络相关配置

本机 Cargo 配置文件：

- `/Users/a111/.cargo/config.toml`

内容：

```toml
[source.crates-io]
replace-with = "rsproxy-sparse"

[source.rsproxy-sparse]
registry = "sparse+https://rsproxy.cn/index/"

[net]
git-fetch-with-cli = true
```

说明：

- 这会影响依赖下载速度和可达性
- 在其他机器复现时，若网络环境不同，可以保留原始 `crates.io`，但建议记录是否使用镜像

## 10. benchmark 输入依赖

本地模型文件路径：

- `llama3_tokenizer/LLM-Research/Meta-Llama-3___1-8B/tokenizer.json`
- `llama3_tokenizer/LLM-Research/Meta-Llama-3___1-8B/original/tokenizer.model`

benchmark 脚本：

- [repeat_single_thread_benchmark.py](/Users/a111/Desktop/code/tokenizers/bindings/python/benches/repeat_single_thread_benchmark.py)

当前主验证口径：

- `threads=1`
- `warmups=1`
- `runs=10`
- `engine=huggingface`
- 数据集：`facebook/xnli`
- split：`train`
- documents：`10000`
- document_mode：`long`

## 11. 已验证的关键命令

重编译 Python 绑定：

```bash
conda run -n tokenizers-bench maturin develop --manifest-path bindings/python/Cargo.toml --release
```

运行单线程 benchmark：

```bash
conda run -n tokenizers-bench python \
  bindings/python/benches/repeat_single_thread_benchmark.py \
  --threads 1 \
  --warmups 1 \
  --runs 10 \
  --engine huggingface
```

运行 ByteLevel 相关 Rust 单测：

```bash
cargo test --manifest-path tokenizers/Cargo.toml byte_level --lib
```

运行关键 Python 回归：

```bash
/Users/a111/miniconda3/envs/tokenizers-bench/bin/python -m pytest \
  bindings/python/tests/bindings/test_tokenizer.py::TestTokenizer::test_encode_batch_fast_text_inputs \
  bindings/python/tests/bindings/test_tokenizer.py::TestTokenizer::test_multithreaded_concurrency
```

## 12. 其他机器复现时建议一并记录

建议在每台复现机器上额外记录：

- CPU 型号
- 物理核 / 性能核 / 能效核数量
- 总内存
- OS 版本与 kernel
- 电源状态
- swap 使用量
- 当前 load average
- Python / Rust / maturin 版本
- `tokenizers-bench` 关键依赖版本
- 是否使用 Cargo / pip 镜像
- benchmark 原始 JSON 输出

这样后续跨机器比较时，不容易把环境差异误当成代码收益。
